{
  "name": "prlens",
  "version": "0.1.0",
  "description": "PRLens - AI-powered CLI tool for reviewing pull requests and understanding codebases.",
  "type": "module",
  "bin": {
    "prlens": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --enable-source-maps --watch dist/index.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "commander": "^14.0.1",
    "dotenv": "^17.2.2",
    "ora": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}

